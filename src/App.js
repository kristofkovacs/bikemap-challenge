import React from "react";
import { ChakraProvider, Flex } from "@chakra-ui/react";

const App = () => {
  return (
    <ChakraProvider>
      <Flex>App</Flex>
    </ChakraProvider>
  );
};

export default App;
